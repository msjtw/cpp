#include<iostream>
#include <vector>

using namespace std;

int main (int argc, char *argv[]) {
    int p;
    cin >> p;
    while (p --> 0) {
        int a, b ,c;
        vector<typename Tp>
        cin >> a >> b >> c;
        for(int i = 0; i < b; i++){

        }
    }
    return 0;
}
