#include<iostream>
#include<vector>
#include<cmath>

using namespace std;

typedef long long int;
typedef pair<int,int> pii;

vector<pii> arr;

int main(){
    int n;
    cin >> n;

    for(int i = 0 ; i < n; i++){
        int a, b;
        cin >> a >> b;
        arr.push_back({a, b});
    }

    for(int i = 0 ; i < n; i++){
        for(int k = 0; k < n; k++){
            if(i == k)
                continue;
            pii 
            long double x = sqrt();
        }
    }

    return 0;
}