#include <iostream>
#include <vector>

using namespace std;

typedef pair<int, int> pii;

const int MX = 200007;
bool bajtoil[MX];

vector<pii>adj[MX];

int main (int argc, char *argv[]) {
    int n, s, m;
    cin >> n >> s >> m;
    for (int i = 0; i < s; i++) {
        int a;
        cin >> a;
        bajtoil[a] = true;
    }
    for (int i = 0; i < m; inc-expression) {
    
    }
    return 0;
}
